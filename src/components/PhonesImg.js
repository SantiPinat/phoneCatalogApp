import React from 'react'

export const PhonesImg = ({img}) => {

    const images =img;

    console.log(images + "ESTE ES EL IMAGES DE PHONES IMG")

    return (
        <div>
{/*             <img src={imgdetails.url} key={imgdetails.id}  className="img-fluid rounded-start p-2 " alt="A phone detail pic" style={{width:100}}/>
 */}        </div>
    )
}
